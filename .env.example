# Environment (development | production)
NODE_ENV=development

# Telegram Bot
# Get from @BotFather
# Optional if WEB_ENABLED=true
TELEGRAM_BOT_TOKEN=

# Master encryption key for private key storage (REQUIRED)
# Generate with: openssl rand -base64 32
# IMPORTANT: Keep this key safe! Losing it means losing access to all encrypted wallets.
# IMPORTANT: Do not change this key after users have created wallets.
MASTER_ENCRYPTION_KEY=

# Solana RPC endpoint
SOLANA_RPC_URL=https://api.devnet.solana.com

# Database mode (sqlite | memory)
# memory = in-memory, data lost on restart
DB_MODE=sqlite

# Path to SQLite database
DATABASE_PATH=./data/bot.db

# Path to mock database (dev only)
MOCK_DATABASE_PATH=./data/mock.db

# Enable web test interface (true | false)
WEB_ENABLED=false

# Port for web interface
WEB_PORT=3000

# Purchase amount in USDC equivalent
DCA_AMOUNT_USDC=6

# Interval between purchases in ms (default: 24h)
DCA_INTERVAL_MS=86400000

# Development wallet (optional, dev mode only)
# Base64-encoded private key to use for all users instead of generating
# DEV_WALLET_PRIVATE_KEY=

# Price source (jupiter | mock)
PRICE_SOURCE=jupiter

# Jupiter API key (required when PRICE_SOURCE=jupiter)
# Get free Basic plan from https://portal.jup.ag/
JUPITER_API_KEY=
